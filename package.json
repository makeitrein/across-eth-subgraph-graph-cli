{
  "name": "across-eth",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ makeitrein/across-eth",
    "create-local": "graph create --node http://localhost:8020/ makeitrein/across-eth",
    "remove-local": "graph remove --node http://localhost:8020/ makeitrein/across-eth",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 makeitrein/across-eth"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
